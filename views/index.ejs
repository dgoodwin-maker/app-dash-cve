<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CVE Tracker</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
</head>
<body class="container py-5">
  <h1 class="mb-4">🛡️ CVE Tracker</h1>

  <form action="/add" method="POST" class="mb-4">
    <div class="row g-2">
      <div class="col-md-3"><input type="text" name="cveId" class="form-control" placeholder="CVE ID" required></div>
      <div class="col-md-5"><input type="text" name="description" class="form-control" placeholder="Description" required></div>
      <div class="col-md-3"><input type="text" name="source" class="form-control" placeholder="Source URL" required></div>
      <div class="col-md-1"><button class="btn btn-primary w-100">Add</button></div>
    </div>
  </form>

  <table class="table table-bordered">
    <thead class="table-dark">
      <tr>
        <th>CVE ID</th>
        <th>Description</th>
        <th>Source</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% cves.forEach(cve => { %>
        <tr>
          <td><%= cve.cveId %></td>
          <td><%= cve.description %></td>
          <td><a href="<%= cve.source %>" target="_blank">View Source</a></td>
          <td>
            <a href="/edit/<%= cve._id %>" class="btn btn-warning btn-sm">Edit</a>
            <form action="/delete/<%= cve._id %>" method="POST" style="display:inline;">
              <button class="btn btn-danger btn-sm" onclick="return confirm('Delete this CVE?')">Delete</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>